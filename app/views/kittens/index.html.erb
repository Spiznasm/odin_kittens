<h1>All Kittens</h1>

<ul class='kittens'>
	<% @kittens.each do |kitten| %>
	<li><%= link_to kitten.name, kitten %>
		<%= link_to 'delete', kitten, method: :delete, data: { confirm: 'Really?' } %>
	</li>
	<% end %>
</ul>
<%= link_to 'New Kitten', new_kitten_path %>